<UserControl x:Class="BudgetsWPF.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:auth="clr-namespace:BudgetsWPF.Authentication"
             xmlns:budgets="clr-namespace:BudgetsWPF.Budgets"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type auth:AuthViewModel}">
            <auth:AuthView></auth:AuthView>
        </DataTemplate>
        <DataTemplate DataType="{x:Type budgets:BudgetsViewModel}">
            <budgets:BudgetsView></budgets:BudgetsView>
        </DataTemplate>
    </UserControl.Resources>
    <ContentControl Content="{Binding CurrentViewModel}"></ContentControl>
</UserControl>
